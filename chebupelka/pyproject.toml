[project]
name = "chebupelka"
version = "0.1.0"
description = "Minimal coding agent"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv.workspace]
members = [
    "текущая/users_crud",
]
